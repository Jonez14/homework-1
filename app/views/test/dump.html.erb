<h1>DUMP</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dump.each do |dump| %>
  <tr>
    <td><%= dump.name %></td>
    <td><%= dump.id %></td>
    <td><%= link_to 'Show', employee %></td>
    <td><%= link_to 'Edit', edit_dump_path(dump) %></td>
    <td><%= link_to 'Destroy', dump, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dump', new_dump_path %>